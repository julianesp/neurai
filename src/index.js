import React from "react";
import ReactDOM from 'react-dom'
import App from "./components/App";

ReactDOM.render( < App / > , document.getElementById('app'))

const dev = 'Alex'

console.log(`El nombre del desarrollador es ${dev}`)